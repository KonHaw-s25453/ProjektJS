# Copy to .env and fill in
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=vcv
PORT=3000

# Optional: port for MySQL/XAMPP (default 3306)
DB_PORT=3306

# Use the file-backed mock DB (no MySQL required) by setting to true
MOCK_DB=false
